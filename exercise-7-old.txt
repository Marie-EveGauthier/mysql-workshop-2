/*Import Database Schema from the following source file into decodemtl_addressbook_import:
* data/import-table-structure.sql
* I will write only the SQL Statement used because they are too many SQL Statement results
*/

mysql> USE decodemtl_addressbook;

mysql> CREATE SCHEMA IF NOT EXISTS `decodemtl_addressbook_import` DEFAULT CHARACTER SET utf8 COLLATE utf8_bin;

mysql> USE `decodemtl_addressbook_import` ;

/* Bulk import data from the following source files into decodemtl_addressbook_import:
* data/import-account.sql
* data/import-addressbook.sql
* data/import-entry.sql
* Again, I will write only the SQL Statement used because they are too many SQL Statement results
*/

mysql> source data/import-table-structure.sql;

mysql> SELECT COUNT(*) FROM Account;
+----------+
| COUNT(*) |
+----------+
|     1000 |
+----------+
1 row in set (0.00 sec)

mysql> source data/import-addressbook.sql;

mysql> SELECT COUNT(*) FROM AddressBook;
+----------+
| COUNT(*) |
+----------+
|     1100 |
+----------+
1 row in set (0.00 sec)


mysql> source data/import-entry.sql;

mysql> SELECT COUNT(*) FROM Entry;
+----------+
| COUNT(*) |
+----------+
|     4000 |
+----------+
1 row in set (0.00 sec)

